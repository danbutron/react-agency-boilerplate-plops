import styles from './{{pascalCase name}}.module.scss';

export interface {{pascalCase name}}Props {}

export const {{pascalCase name}} = ({}: {{pascalCase name}}Props) => {
  return (
    <div className={{{styleClass (camelCase name)}}}>
      {{pascalCase name}} works!
    </div>
  );
};